name = "lunchpick"
compatibility_date = "2024-01-01"
pages_build_output_dir = "build"

[[d1_databases]]
binding = "DB"
database_name = "lunchpick-db"
database_id = ""  # 나중에 채울 예정

# [triggers] crons 는 Pages 미지원 — Cloudflare 대시보드에서 설정
# Workers & Pages > lunchpick > Settings > Cron Triggers > "50 2 * * 1-5"

[vars]
KAKAO_MSG_KEY = ""  # 나중에 채울 예정
